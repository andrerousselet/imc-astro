---
import Head from "../components/HtmlHead.astro";
import Nav from "../components/Nav.astro";

export type Props = {
  title: string;
  description?: string;
};

const {
  title = "Instituto Medicina Cabalista",
  description = "Uma medicina milenar, com um olhar sistêmico e integrativo",
} = Astro.props as Props;
---

<!DOCTYPE html>
<html lang="pt-BR">
  <Head {title} {description} />
  <body>
    <header class="bg-fuchsia-900">
			<div class="mx-auto max-w-7xl flex items-center justify-between p-4 sm:p-6">
				<!-- TODO - replace with logo -->
				<p class="text-gray-200">IMC LOGO</p>
				<Nav />
			</div>
    </header>
    <main class="mx-auto max-w-7xl p-4 sm:p-6">
      <slot />
    </main>
  </body>
</html>
